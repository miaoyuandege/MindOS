# Observability — design only

For maintainers who later need optional read-only observation. The standalone history parser is included, but no live File/Git/System/Runtime observer bundle is shipped. No server connection or installable preset is claimed. Start with [the parser's privacy boundary](../../tools/history-distiller/README.md); live observers need separate genericization and verification.
